{
  "name": "ups-route-map",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that overlays a world map of a UPS package route.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.270",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "jsvectormap": "^1.6.0"
  }
}
